name: demo_pictures_app
description: A new Flutter project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

    # flutter build apk --split-per-abi

environment:
  sdk: '>=3.0.6 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cached_network_image: ^3.2.3
  cupertino_icons: ^1.0.2
  dio: ^5.3.2
  flutter_bloc: ^8.1.3
  provider: ^6.0.5
  url_launcher: ^6.1.12
  path_provider: ^2.1.0
  get: ^4.6.5
  freezed: ^2.4.2       #>>>>>>>>>>   dart run build_runner watch --delete-conflicting-outputs
  freezed_annotation: ^2.4.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  build_runner: ^2.4.6

flutter:
  uses-material-design: true

